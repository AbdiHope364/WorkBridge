# Chat Feature

Owns conversation lists, message views, and real-time transport adapters.

Start with REST history from `@repo/api-client`; add a WebSocket/SSE adapter behind this feature when the backend is ready.
