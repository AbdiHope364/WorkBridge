# Auth Feature

Owns user-facing login, registration, email verification, password reset, and session screens.

Keep API calls in `lib/api.ts` or `@repo/api-client`; keep forms, validation schemas, and view components here.
